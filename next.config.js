/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['ro', 'en'],
        defaultLocale: 'en',
        localeDetection: false
    }
}

module.exports = nextConfig
