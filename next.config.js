/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'ro'],
        defaultLocale: 'ro',
        localeDetection: false
    }
}

module.exports = nextConfig
